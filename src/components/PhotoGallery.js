import React from 'react';
import './PhotoGallery.css'; // Add relevant styles or remove if not needed

const PhotoGallery = () => {
  return (
    <div className="bg-pastel-green p-4 rounded-lg shadow-lg">
      <h2 className="text-xl font-semibold mb-4">Photo Gallery</h2>
      {/* Add your photo gallery content here */}
    </div>
  );
};

export default PhotoGallery;
